dependencies:
  bundler:
    without:
      - default
      - production
      - development
      - test
      - staging
      - benchmark
      - debug

fetch:
  - curl --silent https://shipit.shopify.com/status/version
