#!/usr/bin/env bash

# Output all commands, stop on failures
set -e
set -x

cp -n config/settings{.example,}.yml
cp -n config/ejson-privatekey{.example,}.pem
cp -n config/ejson-publickey{.example,}.pem

bundle install
bundle exec rake db:create
bundle exec rake db:migrate
bundle exec rake db:seed
