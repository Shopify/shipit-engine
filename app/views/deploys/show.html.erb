<section>

<div class="wrapper">
  <header class="section-header">
    <div class="status" data-deploy-status="<%= @deploy.status %>" data-repo-full-name="<%= @deploy.stack.github_repo_name %>">
      <i></i>
    </div>
    <h1>Deploying <span id="deploy-short-sha"><%= @deploy.until_commit.short_sha %></span></h1>
  </header>

</div>

<pre>
  <code data-next-chunks-url="<%= next_chunks_url %>"><%= @deploy.chunk_output %></code>
</pre>
</section>
