<!DOCTYPE html>
<html data-controller="<%= controller_name %>" data-action="<%= action_name %>">
<head>
  <title>Shipit</title>
  <%= stylesheet_link_tag "master", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="layout">
    <header class="sidebar">
      <div class="inner-sidebar">
        <a href="/" class="logo"><i></i><span>Ship it</span></a>
        <nav class="nav">
          <% stacks_by_owner.each do |owner, stacks_for_owner| %>
            <div class="stack">
              <% if stacks_by_owner.keys.length > 1 %>
                <h4 class="stack-owner"><%= owner %></h4>
              <% end %>
              <ul>
                <% stacks_for_owner.group_by(&:repo_name).each do |repo_name, stacks_for_repo| %>
                  <li class="nav-item">
                    <span class="platform"><%= repo_name %></span>
                    <ul class="server-lst">
                      <% stacks_for_repo.each do |stack| %>
                        <li class="server <%= is_current_stack?(stack) ? 'selected' : '' %>">
                          <a href="<%= stack_path(stack) %>">
                            <%= stack.environment %>
                          </a>
                        </li>
                      <% end %>
                    </ul>
                  </li>
                <% end %>
              </ul>
            </div>
          <% end %>
        </nav>
      </div>
    </header>
    <div class="main">
      <%= yield %>
    </div>
  </div>
</body>
</html>
